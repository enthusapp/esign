import React from 'react';

function Player() {
  const style = {
    backgroundColor: 'black',
    color: 'white',
  };

  return (
    <div style={style}>
      <p>
        Text
      </p>
    </div>
  );
}

export default Player;
